[package]
name = "daat-locus-macros"
version = "0.1.0"
edition = "2024"
description = "Compile-time model-facing schema generation for Daat Locus."
license = "Apache-2.0"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
