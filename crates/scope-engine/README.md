# Semantic Code Operation & Propagation Engine

`scope-engine` is an in-process Rust library for project-aware source search,
path-scoped line-hash reads, hash-anchored edits, parse validation, and
propagation review.

It is linked directly by Daat Locus's Coding app. It does not expose or require
a separate service process.
