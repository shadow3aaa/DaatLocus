Apps are encapsulated capability surfaces. Each App provides a distinct functional surface.

Focus an app when a task depends on it, when using it would solve the task better, or when an app emits an important signal for the current task. Use `focus_app` to switch to the target app.
